			<div class="flex flex-col bg-white h-full">
				<div class="flex flex-row justify-between items-center mx-6 my-5">
					<div class="flex flex-row align-center">
						<span class="font-bold">Devices</span>
					</div>
{{ template "button-list.tmpl" . }}
				</div>
				<div class="flex flex-wrap px-4">
{{ range .Device.Children }}
					<div class="cursor-pointer"
						hx-get="/device/{{id}}/detail?childId={{.}}&prevView=full">
	{{ $.Device.RenderChildHTML $.Vars.sessionId . "tile" }}
					</div>
{{- else }}
	{{- template "no-devices.tmpl" . }}
{{- end }}
				</div>
				<div class="flex flex-col px-4">
{{ .Device.RenderChildren .Vars.sessionId }}
				</div>
{{- if and .Vars.demo .Vars.root }}
	{{ template "demo-full.tmpl" . }}
{{- end }}
			</div>
