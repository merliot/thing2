			<div class="flex flex-col bg-white h-full rounded-t-2xl">
				<div class="flex flex-row justify-between items-center mx-6 my-5">
					<div class="flex flex-row align-center cursor-pointer">
						<span class="font-bold">Devices</span>
					</div>
					<img class="icon px-1" src="/device/{{.Device.Id}}/images/tile-view.png"
						hx-get="/device/{{.Device.Id}}/full">
				</div>
				<div class="flex flex-col px-4">
{{ range .Device.Children }}
					<div class="cursor-pointer"
						hx-get="/device/{{$.Device.Id}}/detail?childId={{.}}&prevView=list">
	{{ $.Device.RenderChildHTML $.Vars.sessionId . "/list" }}
					</div>
{{- else }}
	{{- template "no-devices.tmpl" . }}
{{- end }}
				</div>
			</div>
