<div class="flex flex-row">
	<div class="flex flex-col">
		<span>Relay Name</span>
{{- range $i, $relay := .State.Relays }}
	<input class="h-5 mr-2.5 w-36" type="text" placeholder="Relay #{{$i}}" name="relay{{$i}}">
{{- end }}
	</div>
	<div class="flex flex-col">
		<span>GPIO</span>
{{- range $i, $relay := .State.Relays }}
		<select class="h-5" name="gpio{{$i}}">
			<option value="">Select GPIO</option>
	{{- range $key, $target := $.Device.Targets }}
		{{- if eq $key $.Vars.target }}
			{{- range $gpio, $pin := $target.GpioPins }}
			<option value="{{$pin}}">{{$gpio}}</option>
			{{- end }}
		{{- end }}
	{{- end }}
		</select>
{{- end }}
	</div>
</div>
