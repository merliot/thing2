			<div id="{{.Device.Model}}-{{.Device.Id}}" class="panel flex-col h-52 items-center justify-center">
{{- $added := false }}
{{- range $i, $relay := .State.Relays }}
	{{- if and (ne $relay.Gpio "") (ne $relay.Name "") }}
		{{- $added = true }}
				<div class="flex flex-row items-center"
				     hx-post="/device/{{$.Device.Id}}/click?Relay={{$i}}"
				     hx-swap="none">
					<span class="w-16 text-right mx-6">{{$relay.Name}}</span>
					<img class="h-10" src="/model/{{$.Device.Model}}/images/relay-{{$relay.State}}.png">
					<span class="mx-6 px-2.5 text-sm bg-amber-500 text-white rounded">{{$relay.Gpio}}</span>
				</div>
	{{- end }}
{{- end }}
{{- if not $added }}
	{{ template "device-undefined.tmpl" . }}
{{- end }}
			</div>
