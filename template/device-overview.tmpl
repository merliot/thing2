<div class="model-{{model}} {{classOffline}}" id="device-{{id}}"
	hx-target="this"
	hx-swap="outerHTML">

	<div class="flex flex-row ml-{{mult .Level 10}}">

		<div class="view flex flex-row m-1 p-2 min-w-80 justify-between
			{{bgColor}} {{textColor}}
			border-solid border-2 rounded-3xl cursor-pointer"
			hx-get="/device/{{id}}/show-view?view=detail">

			<div class="flex flex-row flex-wrap items-center">
				<span class="text-lg font-bold ml-2.5 w-16">{{name}}</span>
				<div class="rounded-xl overflow-hidden">
{{ template "body-overview.tmpl" .}}
				</div>
			</div>
		</div>
	</div>
</div>
