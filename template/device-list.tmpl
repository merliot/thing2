					<div class="model-{{.Device.Model}}" id="device-{{.Device.Id}}"
						hx-target="this"
						hx-swap="outerHTML">
						<div class="flex flex-col w-36 h-36 m-2.5 rounded-xl border border-solid border-gray-400 overflow-hidden">
							<div class="flex flex-col h-full items-center justify-center">
{{ if ne .Device.DeployParams ""}}
	{{- template "body-list.tmpl" .}}
{{- else}}
	{{- template "device-list-undefined.tmpl" .}}
{{- end}}
							</div>
							<div class="flex flex-row h-10 items-center bg-gray-300">
								<img class="w-5 h-5 mx-3" src="/model/{{.Device.Model}}/images/tile-icon.png">
								<span class="text-sm">{{.Device.Name}}</span>
							</div>
						</div>
					</div>
