					<div class="model-{{model}}" id="device-{{id}}"
						hx-target="this"
						hx-swap="outerHTML">
						<div class="flex flex-col w-36 h-36 m-2.5 rounded-xl border border-solid border-gray-400 overflow-hidden">
							<div class="flex flex-col h-full items-center justify-center">
{{ if ne deployParams ""}}
	{{- template "body-list.tmpl" .}}
{{- else}}
	{{- template "device-list-undefined.tmpl" .}}
{{- end}}
							</div>
							<div class="flex flex-row h-10 items-center">
								<img class="w-5 h-5 mx-3" src="/model/{{model}}/images/tile-icon.png">
								<span class="text-sm">{{name}}</span>
							</div>
						</div>
					</div>
